<% if (post.excerpt) { %>
  <%- post.excerpt %>
<% } else { %>
  <% if(((post.raw.match(/excerpts?:\s*([^\n]+)\n/) || ['', ''])[1])!='\'\'') { %>
    <%= ((post.raw.match(/excerpts?:\s*([^\n]+)\n/) || ['', ''])[1]) %>
  <% } %>
<% } %>